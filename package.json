{
  "name": "react-parcel",
  "version": "1.0.0",
  "description": "Projeto de demonstração do Parcel com React.",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=development parcel index.html -p $PORT",
    "build": "NODE_ENV=production parcel build index.html --no-minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-react-app": "^3.1.0",
    "parcel-bundler": "^1.4.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  }
}
